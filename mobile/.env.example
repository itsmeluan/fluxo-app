# Endereço do backend FLUXO. Em desenvolvimento local com Expo Go no celular,
# "localhost" não funciona — use o IP da sua máquina na rede local (ex: http://192.168.1.10:3000).
EXPO_PUBLIC_API_URL=http://localhost:3000
